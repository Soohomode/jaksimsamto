# ─────────────────────────────────────────────────────────────
# 작심삼토 환경변수 예시
# 실제 값은 .env.local 에 넣는다 (git에 커밋 금지 — .gitignore 처리됨)
# Supabase 대시보드: Project Settings → API / Database
# ─────────────────────────────────────────────────────────────

# 브라우저에 노출되어도 되는 값 (RLS로 보호)
NEXT_PUBLIC_SUPABASE_URL="https://<project-ref>.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="<anon-public-key>"

# 서버 전용 (절대 클라이언트 노출 금지) — 관리 작업/시드에만 사용
SUPABASE_SERVICE_ROLE_KEY="<service-role-key>"

# 관리자 이메일 (쉼표로 여러 명) — /admin 접근 권한
ADMIN_EMAILS="you@example.com"

# Prisma 연결 문자열
# DATABASE_URL: 애플리케이션 런타임용 (커넥션 풀러, 포트 6543, pgbouncer)
# DIRECT_URL:   마이그레이션용 직접 연결 (포트 5432)
DATABASE_URL="postgresql://postgres.<project-ref>:<db-password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.<project-ref>:<db-password>@aws-0-<region>.pooler.supabase.com:5432/postgres"

# Web Push (VAPID) — `npx web-push generate-vapid-keys --json` 로 생성
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:you@example.com"

# 스트릭 리마인더 크론 엔드포인트(/api/cron/streak-reminder) 보호용
CRON_SECRET=""
